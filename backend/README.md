# TinyPal Backend (Mock)

This is a minimal Express server that implements the two endpoints used by the frontend for local testing.

Install and run:

1. Install dependencies

   npm install

2. Start server

   npm start

The server listens on port 4000 by default.
